import requests
import random
import time

# --- ضع رابط موقعك هنا ---
TARGET_URL = "https://your-website.com"  # استبدله بالرابط الذي تريد جلب الزيارات له

# مصادر زيارة وهمية لتبدو طبيعية
REFERERS = [
    "https://www.google.com/",
    "https://www.facebook.com/",
    "https://twitter.com/",
    "https://www.bing.com/"
]

USER_AGENTS = [
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36",
    "Mozilla/5.0 (iPhone; CPU iPhone OS 17_3 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.3 Mobile/15E148 Safari/604.1",
    "Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Mobile Safari/537.36"
]

def send_traffic(count=30):
    print(f"🚀 بدء عملية جلب {count} زيارة...")
    for i in range(count):
        headers = {
            'User-Agent': random.choice(USER_AGENTS),
            'Referer': random.choice(REFERERS)
        }
        try:
            response = requests.get(TARGET_URL, headers=headers, timeout=15)
            print(f"✅ زيارة {i+1} ناجحة | Status: {response.status_code}")
        except Exception as e:
            print(f"❌ فشل في الزيارة {i+1}: {e}")
        
        # انتظار عشوائي لتبدو الزيارة بشرية
        time.sleep(random.randint(2, 5))

if __name__ == "__main__":
    send_traffic()
